TASK COMPLET - Unified Download + iOS perfect mode
Ref: @src/hooks/usePlatform.ts , @src/components/ , @src/lib/offlineStorage

1. CREATE HOOK: @src/hooks/usePlatform.ts
export const usePlatform = () => {
  const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent);
  return { isIOS, isAndroid: !isIOS };
};

2. ANDROID MODE (isIOS = false) - Keep simple:
   - 1 SEUL bouton violet: "Télécharger"
   - Logic: fetch(url) => blob (1 seule fois) => saveOffline(blob) + download to device (URL.createObjectURL)
   - Show green check ✅ when offline saved
   - DELETE blue button "download to app"
   - Applies to: Manga CBZ/CBR/PDF, Videos, Musics, Images

3. iPHONE MODE (isIOS = true) - iPhone only, different UI:
   - DELETE all Android buttons ("Lecteur Android / MX", "Ouvrir dans VLC")
   - Show ONLY 2 buttons:
   
   Button 1 (violet, primary): "Lire"
     -> Opens NLSbox internal reader (NO external app needed)
     -> Video: <video playsInline controls preload="metadata" src={url}>
     -> Manga CBZ/CBR: unzip in browser with JSZip, display images one by one from blob, save to IndexedDB
     -> PDF: pdf.js viewer
     -> Music: <audio controls src={url}>
     -> Image: <img>
     -> Also saves to offline IndexedDB automatically (single fetch)

   Button 2 (gris, secondary): "Ouvrir"
     -> This is the iOS system opener (NOT VLC scheme)
     -> Code:
       if (navigator.share) {
         await navigator.share({ url: fileUrl, title: fileName });
       } else {
         window.open(fileUrl, '_blank');
       }
     -> iOS will automatically show native sheet: "Enregistrer dans Fichiers", "Ouvrir dans Livres" (for PDF), "Ouvrir dans VLC" (if installed), etc.
     -> Do NOT code vlc:// - let iOS handle it.

4. UI RULE:
   if (isIOS) render <IOSActions> with 2 buttons "Lire" + "Ouvrir"
   else render <AndroidActions> with 1 button "Télécharger"

5. IMPORTANT: Never show iOS buttons on Android, never show Android buttons on iOS. Strict separation.

Footer: Keep "Takeoff soon™"